{
    "paths": {
        "/documents": {
            "post": {
                "summary": "Store public data without encryption.",
                "description": "Store public data that doesn't require encryption. This endpoint stores your data as-is and returns a URI (the location of your stored data) and a hash (a fingerprint to verify the data hasn't changed).",
                "requestBody": {
                    "content": {
                        "application/json": {
                            "schema": {
                                "$ref": "./components/schemas.json#/components/schemas/StoreDocumentRequest"
                            },
                            "examples": {
                                "example1": {
                                    "$ref": "./components/examples.json#/components/examples/ExampleRequest1"
                                },
                                "example2": {
                                    "$ref": "./components/examples.json#/components/examples/ExampleRequest2"
                                }
                            }
                        }
                    }
                },
                "responses": {
                    "201": {
                        "description": "Document stored successfully. Returns the storage location and integrity hash.",
                        "content": {
                            "application/json": {
                                "schema": {
                                    "type": "object",
                                    "properties": {
                                        "uri": {
                                            "type": "string",
                                            "description": "The location where your data is stored.",
                                            "example": "http://localhost:3333/api/1.0.0/verifiable-credentials/123e4567-e89b-12d3-a456-426614174000.json"
                                        },
                                        "hash": {
                                            "type": "string",
                                            "description": "A fingerprint to verify the data hasn't changed.",
                                            "example": "computed-hash"
                                        }
                                    }
                                }
                            }
                        }
                    },
                    "400": {
                        "$ref": "./components/responses.json#/components/responses/400"
                    },
                    "401": {
                        "$ref": "./components/responses.json#/components/responses/401"
                    },
                    "409": {
                        "$ref": "./components/responses.json#/components/responses/409"
                    },
                    "500": {
                        "$ref": "./components/responses.json#/components/responses/500"
                    }
                }
            }
        },
        "/credentials": {
            "post": {
                "summary": "Store private data with automatic encryption.",
                "description": "Store private data with automatic encryption. This endpoint encrypts your data before storage — you don't need to encrypt it yourself. The response includes a URI (the location of your stored data), a hash (a fingerprint to verify the data hasn't changed), and a key (your unique decryption key).\n\n**Important**: Save the key securely. Without it, your data cannot be decrypted — **not even by us**.",
                "requestBody": {
                    "content": {
                        "application/json": {
                            "schema": {
                                "$ref": "./components/schemas.json#/components/schemas/StoreDocumentRequest"
                            },
                            "examples": {
                                "example1": {
                                    "$ref": "./components/examples.json#/components/examples/ExampleRequest1"
                                },
                                "example2": {
                                    "$ref": "./components/examples.json#/components/examples/ExampleRequest2"
                                }
                            }
                        }
                    }
                },
                "responses": {
                    "201": {
                        "description": "Document encrypted and stored successfully. Returns the storage location, integrity hash, and decryption key.",
                        "content": {
                            "application/json": {
                                "schema": {
                                    "type": "object",
                                    "properties": {
                                        "uri": {
                                            "type": "string",
                                            "description": "The location where your encrypted data is stored.",
                                            "example": "http://localhost:3333/api/1.0.0/verifiable-credentials/123e4567-e89b-12d3-a456-426614174000.json"
                                        },
                                        "hash": {
                                            "type": "string",
                                            "description": "A fingerprint to verify the data hasn't changed.",
                                            "example": "computed-hash"
                                        },
                                        "key": {
                                            "type": "string",
                                            "description": "Your unique decryption key. **Important**: Save this securely. It is required to decrypt your data and **cannot be recovered if lost**.",
                                            "example": "encryption-key"
                                        }
                                    }
                                }
                            }
                        }
                    },
                    "400": {
                        "$ref": "./components/responses.json#/components/responses/400"
                    },
                    "401": {
                        "$ref": "./components/responses.json#/components/responses/401"
                    },
                    "409": {
                        "$ref": "./components/responses.json#/components/responses/409"
                    },
                    "500": {
                        "$ref": "./components/responses.json#/components/responses/500"
                    }
                }
            }
        }
    }
}
